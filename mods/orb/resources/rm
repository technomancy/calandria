-- -*- lua -*-

local f, env, args = ...
for _,filename in pairs(args) do
   local dir, base = orb.dirname(filename)
   -- TODO: doesn't work with a relative target in a subdir
   f[orb.normalize(dir, env.CWD)][base] = nil
end
